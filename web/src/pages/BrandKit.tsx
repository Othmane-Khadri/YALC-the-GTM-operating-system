// Placeholder — the full brand kit page is wired in a later commit so
// the bootstrap commit stays minimal. Keeping a stub here means routing
// works from the start.
export function BrandKit() {
  return (
    <main className="min-h-screen flex items-center justify-center px-6">
      <p className="text-muted-foreground">Brand kit loading…</p>
    </main>
  )
}
